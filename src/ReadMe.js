import React from "react";

export function ReadMe() {
  return (
    <p>
      This is my playground for doing performant drag/drop and complex state
      management. Take a nested node and try to drag it. Type into the different
      nodes. Is it performant? Is there jank? If so, what's causing it? Does the
      jank depend on the number of nodes?
    </p>
  );
}
